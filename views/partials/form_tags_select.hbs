{{!--added a label--}}

<label for="tags" value="-1">Choose a category</label>
<select name="id_tags" id="tags" class="select">
    <option value="-1">Choose a category</option>
    {{#each tags}}
    <option value="{{this._id}}">{{this.label}}</option>
    {{/each}}
</select> 
